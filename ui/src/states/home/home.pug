div.homepage-hero-module
    div.video-container
        div.filter
        video(autoplay loop class="fillWidth")
            source(src="{{::HomeController.coverVideoUrl + '.mp4'}}" type="video/mp4" ng-bind="'Your browser does not support the video tag. I suggest you upgrade your browser.'")
            source(src="{{::HomeController.coverVideoUrl + '.webm'}}" type="video/webm" ng-bind="'Your browser does not support the video tag. I suggest you upgrade your browser.'")
        div.poster.hidden
            img(src="PATH_TO_JPEG" alt="SITE IMAGE HERE")